/*>sheila.h
 *
 * BeebIt - BBC Micro Model B Emulator
 *
 * (C) Copyright Michael J Foot, 1998-2024
 *
 * Email: <mjfoot.nz@gmail.com>
 *
 * Addressing optimization contributed by Crispian Daniels in 2024
 *
 * Email: <convertedgames@3insdale.me.uk>
 */

struct R6502HOSTMAP;

extern void sheilareset(R6502HOSTMAP* map);

extern struct R6502HOSTMAP* hostconstantread00(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* hostconstantreadFE(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* hostconstantreadFF(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* romselread(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* accconread(struct R6502HOSTMAP* map, unsigned int address_code);

extern struct R6502HOSTMAP* hostnowrite(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* romselwrite_b(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* romselwrite_bp(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* romselwrite_m(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* accconwrite_bp(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* accconwrite_m(struct R6502HOSTMAP* map, unsigned int address_code);
