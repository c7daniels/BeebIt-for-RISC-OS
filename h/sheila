/*>sheila.h
 *
 * BeebIt - BBC Micro Model B Emulator
 *
 * (C) Copyright Michael J Foot, 1998-2024
 *
 * Email: <mjfoot.nz@gmail.com>
 *
 * Addressing optimization contributed by Crispian Daniels in 2024
 *
 * Email: <convertedgames@3insdale.me.uk>
 */

extern void sheilareset(void);

extern HOSTMAP* hostconstantread00(HOSTMAP* hmap, uintptr_t address_code);
extern HOSTMAP* hostconstantreadFE(HOSTMAP* hmap, uintptr_t address_code);
extern HOSTMAP* hostconstantreadFF(HOSTMAP* hmap, uintptr_t address_code);
extern HOSTMAP* romselread(HOSTMAP* hmap, uintptr_t address_code);
extern HOSTMAP* accconread(HOSTMAP* hmap, uintptr_t address_code);

extern HOSTMAP* hostnowrite(HOSTMAP* hmap, uintptr_t address_code);
extern HOSTMAP* romselwrite_b(HOSTMAP* hmap, uintptr_t address_code);
extern HOSTMAP* romselwrite_bp(HOSTMAP* hmap, uintptr_t address_code);
extern HOSTMAP* romselwrite_m(HOSTMAP* hmap, uintptr_t address_code);
extern HOSTMAP* accconwrite_bp(HOSTMAP* hmap, uintptr_t address_code);
extern HOSTMAP* accconwrite_m(HOSTMAP* hmap, uintptr_t address_code);
