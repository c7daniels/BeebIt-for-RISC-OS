/*>8172fdc.h
 *
 * BeebIt - BBC Micro Model B Emulator
 *
 * (C) Copyright Michael J Foot, 1998-2024
 *
 * Email: <mjfoot.nz@gmail.com>
 *
 * Addressing optimization contributed by Crispian Daniels in 2024
 *
 * Email: <convertedgames@3insdale.me.uk>
 */

#define DFS_SECTORS_PER_TRACK 10
#define DFS_SECTOR_SIZE 256
#define ADFS_SECTORS_PER_TRACK 16
#define ADFS_SECTOR_SIZE 256

#define ADFS_MASK 0x10
#define DFS_NONE 0 /*no disc image*/
#define DFS_SSD 1 /*single sided*/
#define DFS_DSD_TRACK 2 /*double sided, track interleave*/
#define DFS_DSD_SIDE 3 /*double sided, side interleave*/
#define ADFS_NON_INTERLEAVED 0x10 /*non interleaved*/
#define ADFS_INTERLEAVED 0x11 /*interleaved*/

/*Intel 8271*/
extern char i8271_data[0x1000]; /*hope we don't get any sectors > 4096 bytes*/

extern void i8271readimage(FILE *hfile, int nlocation, int nlength);
extern void i8271reset(void);
extern struct R6502HOSTMAP* i8271poll(struct R6502HOSTMAP* map);
extern struct R6502HOSTMAP* i8271statusread(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* i8271resultread(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* i8271dataread(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* i8271commandwrite(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* i8271parameterwrite(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* i8271resetwrite(struct R6502HOSTMAP* map, unsigned int address_code);
extern struct R6502HOSTMAP* i8271datawrite(struct R6502HOSTMAP* map, unsigned int address_code);
